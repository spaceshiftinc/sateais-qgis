version: 2
updates:
  # The plugin itself is zero-dependency (stdlib + PyQGIS only), so the only
  # thing to keep current is the GitHub Actions used in workflows.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
